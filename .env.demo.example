POSTGRES_USER=gates
POSTGRES_PASSWORD=replace-with-a-long-password
POSTGRES_DB=gates
GATES_DATA_ROOT=/opt/gates-demo/data

MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=replace-with-a-long-password

# Generate a valid Fernet key with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
AIRFLOW_FERNET_KEY=replace-with-fernet-key
AIRFLOW_SECRET_KEY=replace-with-a-long-random-value
AIRFLOW_ADMIN_USER=admin
AIRFLOW_ADMIN_PASSWORD=replace-with-a-long-password

DATAHUB_MYSQL_ROOT_PASSWORD=replace-with-a-long-password
DATAHUB_MYSQL_PASSWORD=replace-with-a-long-password
DATAHUB_SECRET=replace-with-a-long-random-value
