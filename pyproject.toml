[project]
name = "gates-project-monitoring-pipeline"
version = "1.0.0"
description = "GATES Project Monitoring pipeline — reference implementation (LLD)"
requires-python = ">=3.13"
dependencies = [
    "pandas",
    "pyyaml",
    "requests",
    "openpyxl",
]

# Optional platform dependencies used by the deployed reference implementation:
[project.optional-dependencies]
production = [
    "great-expectations",
    "confluent-kafka",
    "sqlalchemy",
    "psycopg2-binary",
    "pyarrow",
    "trino",           # common/trino_loader.py — lands validated batches into iceberg.staging.*
    "dbt-core",
    "dbt-trino",       # or dbt-spark, depending on the execution engine
    "datahub",
    "apache-airflow",
]

[tool.setuptools]
py-modules = []
