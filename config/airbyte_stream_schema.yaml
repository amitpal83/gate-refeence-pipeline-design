# schemas/project_monitoring.yaml — Airbyte stream schema (JSON Schema, YAML syntax)
# Referenced by airbyte_manifest.yaml's InlineSchemaLoader.schema_ref
"$schema": "http://json-schema.org/draft-07/schema#"
type: object
properties:
  project_id:        { type: string }
  agency:             { type: string }
  region:             { type: string }
  program_area:       { type: string }
  project_title:      { type: string }
  principal_investigator: { type: string }
  start_date:         { type: string, format: date }
  end_date:           { type: [string, "null"], format: date }
  budget_allocated_php: { type: number }
  budget_utilized_php:  { type: number }
  status:             { type: string }
  funding_source:     { type: string }
  last_updated_at:    { type: string, format: date-time }
  
required:
  - project_id
  - agency
  - region
  - program_area
  - start_date
  - budget_allocated_php
  - budget_utilized_php
  - status
  - funding_source
